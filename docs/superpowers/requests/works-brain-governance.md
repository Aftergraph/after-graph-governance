# Build request: WORKS Brain governance

Owner: `Aftergraph/works-execution`
Contract: memory-acc-brain separation binding (`docs/MEMORY-ACC-BRAIN-V1.md` in after-graph-governance)

## What to build

Enforce the WORKS Brain side of the separation binding inside
`Aftergraph/works-execution`: the Brain holds durable organizational
knowledge only — settled evidence, quittance, promoted know-how — with
promotion following the human-stamped promotion law. The Brain never
absorbs live personal context and never substitutes for mission authority.

## Acceptance

Conformance is pinned by the memory-acc-brain separation binding; the
binding's active-doc tests gate the seams. WORKS proves Brain governance
with its own tests, PR, CI, merge queue, and exact-head evidence.

## Out of scope

Governance binds seams only. Governance implements nothing in
`Aftergraph/works-execution`; the canonical Brain behavior belongs to the
WORKS owners.
